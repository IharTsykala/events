import React, { useState } from "react"

// type SearchProps = {
//   dispatch: any,
// }

const StaticPast: React.FunctionComponent = () => {
    return <div className={'static-past'}>Place for static past landing</div>
}

// const mapStateToProps = (state: any) => ({
//   searchStringState: state.pagination.searchString,
// })

export default StaticPast